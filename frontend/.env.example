# LexiLens AI Frontend - Environment Variables
# Copy this file to .env and set the backend URL.

# Backend API base URL (no trailing slash). Never hardcode localhost in production.
VITE_API_URL=http://localhost:5000
